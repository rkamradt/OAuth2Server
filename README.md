# OAuth2Server
Simple oauth2 server with redis backend

** work in progress!

To build:
  mvn clean install

To create Docker image:
  ./dockerbuild.sh

To run:
  docker-compose up

To stop:
  ctrl-c
